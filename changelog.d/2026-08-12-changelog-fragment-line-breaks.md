---
type: changed
area: Architecture
---
Changelog fragment guidance now distinguishes freely placed single newlines for related implementation points from blank lines separating genuinely independent paragraphs or changes.
The changelog fragment and assembled changelog templates no longer use fixed-width hand wrapping.
The fragment template puts the `type` and `area` enums on separate lines so either definition can be found and read independently.

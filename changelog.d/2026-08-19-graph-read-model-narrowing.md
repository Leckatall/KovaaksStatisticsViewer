---
type: changed
area: Graphing
user: Graph data now loads only enabled series and no longer exposes legacy graph mutation controls.
---
`GraphViewModel` and `GraphUseCase` now provide a read-only, enabled-series graph read model; series mutations remain in settings management.

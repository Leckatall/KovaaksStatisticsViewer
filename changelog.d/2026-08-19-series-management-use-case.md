---
type: added
area: Settings
user: Series configuration can now be managed through the settings view model.
---
`SeriesManagementUseCase` owns full series configuration CRUD for `SettingsViewModel`, separating it from the graph read model.

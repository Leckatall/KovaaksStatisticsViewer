---
type: removed
area: Architecture
---
Unmarked commented-out code cleaned up across `app.h`, `file_service.h`, `AppMenuBar.qml`: dead
declarations deleted; `Gallery.qml`'s parked `Showcase` and `SettingsDialog` blocks marked with a
retiring `TODO`; `proto_decoder_test.cpp`'s stale-code marker date format fixed.

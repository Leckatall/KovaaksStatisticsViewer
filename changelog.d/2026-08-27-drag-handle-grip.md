---
type: changed
area: User Interface
user: Series configuration rows now use a contrasting background, a brighter six-dot grip, hover highlights, and compact expression-editor controls to make their editing affordances clear.
---
`SeriesConfigEditorDelegate` renders the existing 12x24 drag target as a centred 2-column by 3-row dot matrix, preserving its drag handler and reorder behaviour. Its rounded surface rests at a darkened window shade, lightens on hover, and renders the grip dots with a lighter palette shade. The expression editor action now uses a `ƒₓ` glyph with a tooltip and explicit accessible name.

---
type: added
area: Documentation
---
Added conventions for Architecture Decision Records and the managed Architecture Description corpus, including record structure, supersession rules, evidence standards, document ownership, and diagram guidance.
Added `scripts/arch_doc.py` and its pytest suite to validate and manage architecture documents, views, references, root facts, and ADR relationships through deterministic repository operations; `pyproject.toml` and `uv.lock` pin the tool's Python environment.
